# RAG observability

RAG observability covers retrieval duration, dense and lexical candidate counts, fused candidates, reranker threshold, insufficient-evidence rate, active index version, and retrieval circuit state. Default logs include request/query IDs and error class, not raw queries or document chunks. The root overview may aggregate these metrics without redefining them or labeling them CSAT.
